<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Upload Memory</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>

    <!-- ✨ Page Title -->
    <h1 class="title">Upload New Memory</h1>

    <div class="form-container">
        <form action="/upload" method="POST" enctype="multipart/form-data">
            
            <!-- 🖼️ Image Upload -->
            <div class="form-group">
                <label for="image" class="form-label">Upload Image:</label>
                <input type="file" id="image" name="image" accept="image/*" required class="form-input">
            </div>

            <!-- 🎵 Audio Upload -->
            <div class="form-group">
                <label for="audio" class="form-label">Upload Audio:</label>
                <input type="file" id="audio" name="audio" accept="audio/*" required class="form-input">
            </div>

            <!-- 📝 Content Upload -->
            <div class="form-group">
                <label for="content" class="form-label">Content:</label>
                <textarea id="content" name="content" required class="form-textarea"></textarea>
            </div>

            <!-- 🚀 Submit Button -->
            <div class="form-group">
                <button type="submit" class="form-button">Upload</button>
            </div>
            
        </form>
    </div>

</body>
</html>
